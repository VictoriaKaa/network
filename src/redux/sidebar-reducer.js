
let initialState = {
        friendsData: [
            { id: 1, name: 'Andrey' },
            { id: 2, name: 'Dimas' },
            { id: 3, name: 'Vika' }
        ]
}

const sidebarReducer = (state = initialState, action) => {
    
    return state;
}
export default sidebarReducer;